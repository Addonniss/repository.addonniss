v2.4.16
- Fixed OpenAI GPT-5 mini translations failing immediately because the API rejects custom temperature values for that model
- GPT-5 mini now uses OpenAI's accepted default generation settings, with no custom effort or verbosity requested by Translatarr

v2.4.15
- Added Anthropic Claude as a new AI provider with Claude Haiku 4.5, Claude Sonnet 4.6, and Claude Opus 4.7 model options
- Expanded Gemini model selection with Gemini 2.5 Pro and Gemini 2.5 Flash-Lite, while keeping Gemini 2.5 Flash as the default and Fast Mode - Gemini 2.5 Flash as a separate UX-oriented option

v2.4.14
- Fixed Kodi settings visibility so Translatarr settings are available consistently at Basic level

v2.4.13
- Embedded subtitle extraction now skips streamed sources that do not expose a real file path, instead of attempting remote extractor probe/extract calls on transient `plugin://`, `http://`, or `https://` playback URLs
- Added a user-facing notification when embedded extraction is unavailable because playback does not expose a real file path

v2.4.12
- Fixed sidecar subtitle matching to prefer the real playing filename/path instead of `Player.Title`, so existing target-language subtitles next to the video are detected more reliably
- Auto mode now also uses the real playing filename/path when saving translated subtitles, falling back to `Player.Title` only when no real media path is available

v2.4.11
- Remote Extractor Timeout now drives both the Kodi-side wait and the real remote probe/extraction commands on the server, removing the previous timeout mismatch
- Updated Remote Extractor settings text and README guidance so `/probe`, `/extract`, and timeout behavior are documented more clearly
- Refreshed roadmap and remote-extractor documentation to match the verified production workflow, including `dav://` path mapping and nzbdav / rclone-backed media notes

v2.4.10
- Improved embedded subtitle extraction workflow with clearer Force Extraction behavior for users who want to retranslate even when an embedded target-language subtitle already exists
- Manual mode now checks next-to-video subtitles before embedded extraction fallback, so valid sidecar source and target subtitles are reused instead of triggering unnecessary extraction
- Added better extraction UX with start/failure/timeout notifications and increased the default remote extractor timeout to 8 minutes for slower remote or symlink-backed media
- Refined embedded extraction settings layout, provider-specific temperature visibility, and remote extractor wording for a clearer Kodi settings experience
- Reduced repeated debug-log spam from stale subtitle checks while preserving useful per-playback diagnostics

v2.4.9
- Added first public support for Translatarr Remote Extractor as a companion service for embedded subtitle extraction when local tools are unavailable or impractical
- Integrated remote embedded extraction into the normal Translatarr subtitle flow so extracted source subtitles are written into the standard Translatarr subtitle folder and continue through normal translation handling
- Added remote target-language embedded subtitle probing so Translatarr can skip source extraction and translation remotely when the selected target language already exists as an embedded subtitle track
- Fixed subtitle language classification so target-language files such as `.target-language.sdh.srt` and similar suffix variants are no longer treated as source subtitles
- Updated embedded extraction settings and wording for clearer local-vs-remote behavior, added user-visible extraction-start notifications, and documented the new Translatarr Remote Extractor workflow in the README

v2.4.8
- Added a simple service on/off setting so users can pause Translatarr directly from addon settings without enabling or disabling the Kodi addon itself
- Expanded embedded subtitle extraction to support both MKV and MP4 files, using MKVToolNix tools for MKV and FFmpeg tools for MP4
- Embedded subtitle fallback now also works in Auto mode and can skip translation entirely when the selected target language already exists as an embedded subtitle
- Reorganized embedded subtitle options into their own settings group and simplified external tool configuration to MKVToolNix and FFmpeg executable-folder settings

v2.4.7
- Improved embedded subtitle extraction logging so Manual mode now shows resolved extraction paths, selected tool steps, and explicit timeout failures instead of appearing stuck
- Added longer network-path extraction timeouts for UNC-backed playback so slow SMB/Windows share extractions have more time to finish
- Updated README guidance for embedded subtitle extraction with practical SMB/UNC expectations, observed network performance notes, and clearer limitations for network-backed files

v2.4.6
- Added optional embedded subtitle extraction in Manual mode for local MKV files when no external source subtitle is found yet
- Embedded extraction now looks for source-language subtitle tracks, prefers non-SDH tracks when available, and writes a standard source `.srt` into the configured manual subtitle folder
- Added settings for enabling embedded subtitle extraction and optional tool paths for `mkvinfo`, `mkvextract`, and `ffmpeg`
- Manual mode now allows pre-existing source subtitles to proceed when no matching translated target subtitle exists yet
- Manual mode now skips stale pre-existing source subtitles only when a matching translated target subtitle for the current video already exists

v2.4.5
- Expanded language coverage across Gemini, OpenAI, DeepL, and LibreTranslate pickers with Bengali, Bulgarian, Estonian, Indonesian, Latvian, Lithuanian, Serbian, Slovak, Swahili, and Ukrainian
- Added missing DeepL-supported languages that were previously omitted from the DeepL-specific picker list, including Thai, Vietnamese, Catalan, Croatian, Hebrew, and Hindi

v2.4.4
- Added optional Dual-Language Display so subtitles can show source text above the translated text
- Dual-Language Display is disabled by default and lives in the Mode group as an additive viewing option

v2.4.3
- Added optional SDH/HI cue removal to strip common hearing-impaired subtitle cues while preserving spoken dialogue
- Improved SDH/HI cleanup to handle full-line, leading, and trailing cue fragments without breaking multiline subtitle structure
- Added shared localization guidance for Gemini and OpenAI prompts to improve idioms and gender-aware phrasing
- Improved DeepL usage statistics fallback so character consumption no longer shows as zero when DeepL omits billed-character details
- Renamed settings categories for clarity: Mode, Provider, Translation, Advanced

v2.4.2
- Added LibreTranslate as a free self-hosted translation provider for offline or home-network use
- LibreTranslate support lets users keep subtitle translation on their own server for more privacy and independence from paid cloud APIs
- LibreTranslate can be very fast on a local network and gives users another no-cost option alongside DeepL Free
- Added dedicated LibreTranslate URL, optional API key, and separate source/target language settings
- Added LibreTranslate setup guidance, including full URL requirement and server language-loading notes

v2.4.1
- Added DeepL Free machine translation as a very fast provider with 500,000 free characters per month, enough for roughly 9 full movies on average
- Added provider-aware language selection so DeepL only shows supported languages
- Fixed DeepL request payload rejection during chunk translation
- Updated addon metadata and README for DeepL support

v2.4.0
- Live translation is now always enabled for a faster playback experience
- Removed the Real-Time Translation toggle from settings
- Auto mode now reuses translated subtitles from the Translatarr subtitles folder across playback sessions

v2.3.14
- Fixed auto mode playback session detection for temp subtitle scanning
- Improved compatibility with reused generic temp subtitle filenames in Kodi temp folders 
- AKA OPENSUBTITLES overwritting the same tempsubtitle file fixed finally!!! ENJOY!
- Success statistics popup now shows where the translated subtitle was saved

v2.3.12
- Auto mode now also detects Kodi Custom subtitle folder when active
- Auto mode continues to scan next-to-video and temp subtitle locations
- Auto mode now discovers Kodi temp subfolders dynamically under `special://temp/`
- Improved detection of provider temp subtitles with generic names like `TempSubtitle.*.srt`
- Improved handling for UUID-style subtitle filenames in Kodi custom subtitle folders
- Manual mode now safely falls back to recent-session generic or UUID-style subtitle filenames in the configured folder
- Manual mode still prioritizes exact video-name subtitle matches before using the recent-session fallback
- Reduced false positives from stale reused temp subtitle files across playback sessions
- Manual mode now reuses the shared subtitle session-age tolerance window
- Active translation now aborts if the source subtitle file changes mid-job
- Improved path resolution for plugin playback that exposes real SMB/local paths
- Reduced Kodi subtitle setting log noise during active polling
- Subtitle polling now runs only during video playback
- Gemini 2.5 Flash Fast Mode added
  * uses Gemini 2.5 Flash with thinking budget disabled
  * still respects the user-configured temperature
- Gemini 2.5 Flash is now the default Gemini model for new selections
- Gemini 2.0 Flash and Gemini 1.5 Flash are now marked as legacy in settings
- Preserved compatibility with existing saved Gemini model selections

v2.3.9
- Major stability improvements for Real-Debrid / Torbox streams
- Improved subtitle detection in temp folders
- Prevented duplicate translations and unnecessary API usage
- Added safer VFS path handling across platforms
- Improved subtitle write detection to avoid incomplete files

v2.3.6
- Fixed Kodi crash on plugin:// and http:// sources 
  * works now with Fenlight/POV plugins streaming on Real Debrid / Torbox
  * works with "local" WebDav folders / like nzbDav

v2.3.0
- Added Auto Mode
  * no other mandatory setting in Kodi is required
  * you don't need Custom Folder Location set-up
  * we are polling temp folders and movie folder location

v2.2.0

- Added Live Translation Mode (Optional)
  * Subtitles progressively update during translation
  * Reloads partial translations at defined progress points
  * Reduces perceived wait time on large subtitles

- Instant Source Subtitle Display
  * Newly downloaded source subtitles are shown immediately during playback
  * Translation runs in background without blocking viewing

- Single Instance Service Protection
  * Prevents multiple service instances from running simultaneously
  * Eliminates duplicate polling and race conditions
  * Improves overall addon stability

- Improved Candidate Detection Logic
  * More flexible subtitle filename matching
  * Maintains strict ISO source/target validation
  * Preserves size-based overwrite detection

- Enhanced Session Safety
  * Maintains session-based overwrite protection
  * Prevents cross-session unwanted retranslations
  * Keeps API usage controlled and predictable

- Improved Global Logging System
  * Centralized debug logging
  * More consistent internal state reporting
  * Better traceability during live translation

- Internal Code Refactoring
  * Cleaner subfolder handling
  * Safer monitor lifecycle management
  * Structural stability improvements without altering core behavior


v2.1.1
- Fix for target language display in stats and notifications


v2.1.0

- Added Global Translation Lock
  * Prevents overlapping translations 
  * Ensures only one translation runs at a time
  * Eliminates duplicate API calls on rapid subtitle changes

- Refined Source Change Detection
  * Source overwrite detection now based on file size only
  * Prevents unnecessary retranslation when identical subtitles are re-downloaded

- Session-Based Translation Protection
  * Existing translated subtitles are never auto-overwritten across sessions
  * Protects user-approved translations

- Smart Mid-Session Overwrite Logic
  * Retranslation allowed only within the same playback session
  * Subtitle switch/download during playback triggers safe retranslation

- Improved Cross-Session Behavior
  * Playback restart no longer triggers unwanted retranslation
  * Prevents API cost waste
  
- Temporary translation files
   * Translations are first saved to temporary files
   * Only completed translations overwrite the final subtitles, keeping your folder clean

- Improved Debug Logging
  * Clear logging for overwrite decisions
  * Logs when translation is skipped due to active lock

--------------------------------------------------

v2.0.1

- Adaptive chunk behavior may help free tier users
  * Starts with user-defined chunk size
  * Halves chunk size per retry if API rejects
  * Retries up to 3 times per chunk

- Strict ISO language validation
  * Exact source ISO matching
  * Target ISO detection prevents retranslation
  * Removed ambiguous matching

- Removed Target Auto-Detect
  * Target language must be explicitly selected

- Improved playback restart handling
- Service cleanup and monitor improvements
- Added Catalan language support
- Improved settings readability
- Debug mode improvements
- Added Change Log viewer in settings

--------------------------------------------------

v2.0.0

- Multi-AI support (Gemini + OpenAI)
- Added OpenAI models: gpt-4o, gpt-4o-mini, gpt-5-mini
- Translation styles: Family-Friendly, Natural, Gritty / Adult
- Live settings reload
- Smart chunk retry system
- Cost transparency per translation
- Debug mode added
- Improved subtitle detection
- Better API error handling

--------------------------------------------------

v1.0.0

- Initial release
- Added Gemini models: 2.0 Flash, 1.5 Flash, 2.5 Flash
- Optional statistics popup
- Simple notification mode
